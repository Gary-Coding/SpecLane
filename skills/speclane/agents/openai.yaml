interface:
  display_name: "SpecLane"
  short_description: "支持 OpenSpec 桥接的结构化 AI 研发交付工作流"
  default_prompt: "读取当前工作空间中的 workspace.yml 和 ~/.speclane/skill-config.yml，通过 speclane 处理 /sl:* 工作流命令，严格按 propose、bridge、plan、实现、自查、审查、验证、通知和归档状态机推进。"
